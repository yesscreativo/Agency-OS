/** Preset compartido. Paleta real pendiente de Docs/95-Assets/Colors.md (hoy vacío) —
 * placeholder basado en la identidad visual del cotizador actual (Sora + DM Mono). */
/** @type {import("tailwindcss").Config} */
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        sans: ["Sora", "sans-serif"],
        mono: ["DM Mono", "monospace"],
      },
    },
  },
  plugins: [],
};
